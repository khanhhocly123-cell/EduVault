# EduVault

App chính nằm tại `web/`; `phase0/` là pipeline OCR/render dùng phía sau.

```powershell
cd web
npm install
npm start
```

Mở `http://localhost:5173/index.html`.

- Trạng thái, cách chạy và backlog Phase 1: `web/README.md`
- Kế hoạch sản phẩm: `PLAN.md`
- Pipeline/model và kill test: `phase0/README.md`
